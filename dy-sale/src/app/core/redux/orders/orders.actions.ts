import { createAction } from '@ngrx/store';

export const increment = createAction('[Order Component] Increment');
export const decrement = createAction('[Order Component] Decrement');
export const reset = createAction('[Order Component] Reset');